# Azure OpenAI Configuration
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!

# Primary Model
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com
AZURE_OPENAI_KEY=your-api-key-here
AZURE_OPENAI_DEPLOYMENT=gpt-4
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Secondary Model (Optional)
AZURE_OPENAI_ENDPOINT_2=https://your-secondary-resource.cognitiveservices.azure.com
AZURE_OPENAI_KEY_2=your-secondary-api-key-here
AZURE_OPENAI_DEPLOYMENT_2=gpt-4
AZURE_OPENAI_API_VERSION_2=2024-12-01-preview

# Azure AD Configuration (Required for querying user's Azure environment)
# Create an App Registration in Azure Portal:
# 1. Go to Azure Portal > Azure Active Directory > App Registrations > New Registration
# 2. Name: "Azure Monitor Cost Optimizer" (or your choice)
# 3. Supported account types: "Accounts in any organizational directory"
# 4. Redirect URI: Select "Single-page application (SPA)" and enter "http://localhost:3000"
# 5. After creation, copy the Application (client) ID below
# 6. Go to API Permissions and add:
#    - Microsoft Graph > Delegated > User.Read
#    - Azure Service Management > Delegated > user_impersonation
#    - Log Analytics API > Delegated > Data.Read
# 7. Grant admin consent for your organization
AZURE_AD_CLIENT_ID=your-app-registration-client-id

# Server Configuration
PORT=3000
